# 竞品分析 Demo 部署说明

## 1. 项目内容

这是一个纯前端静态 Demo，核心文件为：

- `index.html`
- `css/style.css`
- `js/mockData.js`
- `js/main.js`
- `assets/`

## 2. 本地运行

优先使用静态服务启动：

```bash
npx serve .
```

然后访问终端输出的本地地址即可。

## 3. Netlify 部署

1. 打开 Netlify 控制台。
2. 选择 `Add new site` -> `Deploy manually`。
3. 将整个项目打包后的静态目录或压缩包解压目录上传。
4. `Publish directory` 选择项目根目录。
5. 部署完成后会得到一个公网链接。

## 4. demogo.cn 部署

1. 登录 demogo.cn。
2. 新建静态站点。
3. 上传项目根目录中的静态文件。
4. 站点发布后复制公网访问链接。

## 5. 注意事项

- 页面里的 `api/local/*`、`api/mcp/*` 这类接口，只有在你自己有可访问后端时才会生效。
- 如果只是公开展示 Demo，静态页面和内置模拟数据可以直接用。
- 如果要让别人连接自己的 MCP/API，发布后的站点必须能访问对方提供的公网接口。

## 6. 分享链接

部署到公网后，页面右上角“分享配置”生成的链接就可以直接发给别人。
当前本机 `127.0.0.1` 地址只能自己在本机打开，不能给别人直接访问。
